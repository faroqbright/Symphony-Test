<form method="post" action="{{ path('app_invoiceline_delete', {'id': invoice_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invoice_line.id) }}">
    <button class="btn">Delete</button>
</form>
