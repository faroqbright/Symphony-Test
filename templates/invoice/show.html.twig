{% extends 'base.html.twig' %}

{% block title %}Invoice{% endblock %}

{% block body %}
<div class="container">
    <h1>Invoice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invoice.id }}</td>
            </tr>
            <tr>
                <th>InvoiceDate</th>
                <td>{{ invoice.InvoiceDate ? invoice.InvoiceDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>InvoiceNumber</th>
                <td>{{ invoice.InvoiceNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_invoice_index') }}">back to list</a>

    <a href="{{ path('app_invoice_edit', {'id': invoice.id}) }}">edit</a>

    {{ include('invoice/_delete_form.html.twig') }}
</div>
{% endblock %}
